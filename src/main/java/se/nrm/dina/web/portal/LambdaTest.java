/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package se.nrm.dina.web.portal;

/**
 *
 * @author idali
 */
public class LambdaTest {
    
    public static void main(String[] args) {
        //            Map<String, Integer> map = counts.stream().filter(x -> ((Integer.parseInt(x.getName())) >= fromYear))
//                .collect(Collectors.toMap(
//                    m -> m.getName(),
//                    m -> (int)m.getCount() 
//                ));
            
//            System.out.println("start");
//            int total = counts.stream()
//                    .filter(x -> ((Integer.parseInt(x.getName())) < fromYear))
//                    .mapToInt(x -> (int) x.getCount()).sum();
//            System.out.println("end");
    }
}
